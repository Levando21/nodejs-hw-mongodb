tags:
  - Contacts
summary: Patch contact by id
operationId: updateContact
description: Patch contact by unique identifier
security:
  - bearerAuth: []
parameters:
  - name: contactId
    in: path
    required: true
    schema:
      type: string
      example: '66a35382997eac73e992ae30'
    description: The ID of the contact to update
requestBody:
  content:
    multipart/form-data:
      schema:
        type: object
        properties:
          name:
            description: 'The name of the contact'
            type: string
            example: 'Jim Malkovich'
          phoneNumber:
            description: 'The phone number of the contact'
            type: string
            example: '+1234567890'
          email:
            description: 'The email of the contact'
            type: string
            example: 'jimmalkov@gmail.com'
            default: false
          contactType:
            description: 'Specifies the type of contact'
            type: string
            enum: ['work', 'home', 'personal']
            example: 'work'
            default: 'personal'
          photo:
            description: 'The photo of the contact'
            type: string
            format: binary
          isFavourite:
            description: 'Indicates whether the contact is marked as a favorite. True if the contact is a favorite, false otherwise.'
            type: boolean
        additionalProperties: false
    application/json:
      schema:
        type: object
        required:
          - name
          - phoneNumber
          - contactType
        properties:
          name:
            description: 'The name of the contact'
            type: string
            example: 'Jim Malkowich'
          phoneNumber:
            description: 'The phone number of the contact'
            type: string
            example: '+1234567890'
          email:
            description: 'The email of the contact'
            type: string
            example: 'jimmalkov@gmail.com'
          isFavorite:
            description: 'Indicates whether the contact is marked as a favorite'
            type: boolean
            example: true
            default: false
          contactType:
            description: 'Specifies the type of contact'
            type: string
            enum: ['work', 'home', 'personal']
            example: 'work'
            default: 'personal'
  description: 'Fields to be updated for the contact. Only the fields provided will be updated.'
responses:
  '200':
    description: 'Successful operation'
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: 'Successfully updatedd contact with id 69a35382997ucc73e992ie88!'
            data:
              type: object
              $ref: ../../../components/schemas/contact.yaml
  '400':
    $ref: ../../../components/responses/400.yaml
  '401':
    $ref: ../../../components/responses/401.yaml
  '404':
    $ref: ../../../components/responses/404.yaml
  '500':
    $ref: ../../../components/responses/500.yaml
