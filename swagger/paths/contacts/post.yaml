tags:
  - Contacts
summary: Create a new contact
operationId: createContacts
description: 'Create a contact with payload'
security:
  - bearerAuth: []
requestBody:
  content:
    multipart/form-data:
      schema:
        type: object
        required:
          - name
          - phoneNumber
          - contactType
        properties:
          name:
            description: 'The name of the contact'
            type: string
            example: 'John Doe'
          phoneNumber:
            description: 'The phone number of the contact'
            type: string
            example: '+1234567890'
          email:
            description: 'The email of the contact'
            type: string
            example: 'johndoe@gmail.com'
          contactType:
            description: 'Specifies the type of contact'
            type: string
            enum: ['work', 'home', 'personal']
            example: 'work'
            default: 'personal'
          photo:
            description: 'The photo of the contact'
            type: string
            format: binary
          isFavourite:
            description: 'Indicates whether the contact is marked as a favorite. True if the contact is a favorite, false otherwise.'
            type: boolean

    application/json:
      schema:
        type: object
        required:
          - name
          - phoneNumber
          - contactType
        properties:
          name:
            description: 'The name of the contact'
            type: string
            example: 'John Doe'
          phoneNumber:
            description: 'The phone number of the contact'
            type: string
            example: '+1234567890'
          email:
            description: 'The email of the contact'
            type: string
            example: 'johndoe@gmail.com'
          isFavorite:
            description: 'Indicates whether the contact is marked as a favorite'
            type: boolean
            example: true
            default: false
          contactType:
            description: 'Specifies the type of contact'
            type: string
            enum: ['work', 'home', 'personal']
            example: 'work'
            default: 'personal'
responses:
  '201':
    description: 'Create a contact'
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 201
            message:
              type: string
              example: 'Successfully created a contact for {userName}!'
            data:
              type: object
              $ref: ../../components/schemas/contact.yaml
  '400':
    $ref: ../../components/responses/400.yaml
  '401':
    $ref: ../../components/responses/401.yaml
  '500':
    $ref: ../../components/responses/500.yaml
